"""
Rich日志配置

使用Rich库提供美观的日志输出

Rich是一个Python库，用于在终端中创建丰富的文本和美观的格式。
这个模块配置了全局日志处理器，使用Rich的RichHandler。

作者: Liu Gang (原作者)
日期: 2025-12-20
修订: 添加详细注释
"""

import logging
from rich.logging import RichHandler


# ============================================================================
# 日志配置
# ============================================================================

# 日志格式（仅显示消息内容，Rich会自动添加时间戳和级别）
FORMAT = "%(message)s"

# 配置全局日志
logging.basicConfig(
    level="NOTSET",           # 记录所有级别的日志
    format=FORMAT,            # 日志格式
    datefmt="[%X]",          # 时间格式（仅显示时:分:秒）
    handlers=[RichHandler()] # 使用Rich处理器
)


# ============================================================================
# 使用说明
# ============================================================================

"""
使用方法：

1. 在模块开头导入：
   >>> import logging
   >>> from mujoco_env.utils import rich_logger

2. 创建logger：
   >>> logger = logging.getLogger(__name__)

3. 使用logger：
   >>> logger.info("这是一条信息")
   >>> logger.warning("这是一条警告")
   >>> logger.error("这是一条错误")

特点：
- 彩色输出
- 自动高亮
- 美观的格式
- 支持Markdown
- 支持表格
- 支持进度条

日志级别：
- DEBUG: 详细的调试信息
- INFO: 一般信息
- WARNING: 警告信息
- ERROR: 错误信息
- CRITICAL: 严重错误
"""


# ============================================================================
# 模块导出
# ============================================================================

__all__ = []  # 此模块主要用于副作用（配置日志）

